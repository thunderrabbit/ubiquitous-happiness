# server_name is used to create VPC, security group, and to tag the instance
server_name: "Zcash"

## TODO: make region be specified by user when setup.sh is run
region: ap-northeast-1

# The image depends on the region
image: ami-dbc0bcbc  # Debian 8
#image: ami-afb09dc8  # Ubuntu 16

# Server is determined by ami above
tag_os: "Debian 8"
